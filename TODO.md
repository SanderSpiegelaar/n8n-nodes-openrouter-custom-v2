# TODO

- [ ] Prepopulate the Structured Output Prompt Template with the default prompt
- [ ] Rename node output to `output` and:
  - [ ] disable default output of text and structured (only output)
  - [ ] remove default output of response
  - [ ] add toggle to output response object in node output
- [ ] Reorder node properties so prompt comes first
